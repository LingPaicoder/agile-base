package com.lpcoder.agile.base.util

/**
 * @author: liurenpeng
 * @date: Created in 18-7-12
 */
object NumberUtil {

    // Byte

    fun isEq(target: Byte?, norm: Byte?) = target!! == norm!!
    fun isGt(target: Byte?, norm: Byte?) = target!! > norm!!
    fun isGte(target: Byte?, norm: Byte?) = target!! >= norm!!
    fun isLt(target: Byte?, norm: Byte?) = target!! < norm!!
    fun isLte(target: Byte?, norm: Byte?) = target!! <= norm!!

    // Short

    fun isEq(target: Short?, norm: Short?) = target!! == norm!!
    fun isGt(target: Short?, norm: Short?) = target!! > norm!!
    fun isGte(target: Short?, norm: Short?) = target!! >= norm!!
    fun isLt(target: Short?, norm: Short?) = target!! < norm!!
    fun isLte(target: Short?, norm: Short?) = target!! <= norm!!

    // Int

    fun isEq(target: Int?, norm: Int?) = target!! == norm!!
    fun isGt(target: Int?, norm: Int?) = target!! > norm!!
    fun isGte(target: Int?, norm: Int?) = target!! >= norm!!
    fun isLt(target: Int?, norm: Int?) = target!! < norm!!
    fun isLte(target: Int?, norm: Int?) = target!! <= norm!!

    // Long

    fun isEq(target: Long?, norm: Long?) = target!! == norm!!
    fun isGt(target: Long?, norm: Long?) = target!! > norm!!
    fun isGte(target: Long?, norm: Long?) = target!! >= norm!!
    fun isLt(target: Long?, norm: Long?) = target!! < norm!!
    fun isLte(target: Long?, norm: Long?) = target!! <= norm!!

    // Float

    fun isEq(target: Float?, norm: Float?) = target!! == norm!!
    fun isGt(target: Float?, norm: Float?) = target!! > norm!!
    fun isGte(target: Float?, norm: Float?) = target!! >= norm!!
    fun isLt(target: Float?, norm: Float?) = target!! < norm!!
    fun isLte(target: Float?, norm: Float?) = target!! <= norm!!

    // Double

    fun isEq(target: Double?, norm: Double?) = target!! == norm!!
    fun isGt(target: Double?, norm: Double?) = target!! > norm!!
    fun isGte(target: Double?, norm: Double?) = target!! >= norm!!
    fun isLt(target: Double?, norm: Double?) = target!! < norm!!
    fun isLte(target: Double?, norm: Double?) = target!! <= norm!!
}